# Managed by Ansible (cis_baseline role).
[DEFAULT]
bantime  = {{ cis_baseline_fail2ban_bantime }}
findtime = {{ cis_baseline_fail2ban_findtime }}
maxretry = {{ cis_baseline_fail2ban_maxretry }}
backend  = systemd

[sshd]
enabled = true
port    = ssh
